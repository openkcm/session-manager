{{- if .Values.apiServer.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "session-manager.fullname" . }}
  labels:
    {{- include "session-manager.api-server.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "session-manager.fullname" . }}
  minReplicas: {{ .Values.apiServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.apiServer.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.apiServer.hpa.targetCPUUtilizationPercentage }}  # Target 70% CPU usage per pod
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.apiServer.hpa.targetMemoryUtilizationPercentage }}  # Target 80% Memory usage per pod
{{- end }}
